// space complexity
function sum(arr){
    let total=0
    for(let i=0;i<arr.length;i++){
        total+=arr[i].
    }
    return total
}
console.log(sum()) ////O(1)

function double(arr){
    let newArr=[]
    for (let i = 0; i < arr.length; i++) {
        newArr.push(2* arr[i]);
        
    }
    return newArr}

    console.log(double([1,2,3]))
    // 2,4,6